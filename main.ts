let quote: string = "A person never made a mistake never tried anything";
let famous_person: string = "Albert Einstein";
let message: string =  `${famous_person} once said, ${quote}`;
console.log(message);